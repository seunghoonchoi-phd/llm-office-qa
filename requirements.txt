python-pptx>=1.0
openpyxl>=3.1
python-docx>=1.1
Pillow>=10.0

# Optional: enables PNG export from tools/render_powerpoint.py (PDF rendering works without it,
# and needs LibreOffice installed separately on the system).
PyMuPDF>=1.24
